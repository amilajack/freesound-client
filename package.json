{
  "name": "freesound-client",
  "version": "0.2.1-3",
  "main": "./dist/index.js",
  "author": "Amila Welihinda <amilajack@gmail.com>",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@tsconfig/node10": "^1.0.7",
    "@types/jest": "^26.0.12",
    "@types/node": "^14.6.2",
    "cross-env": "^7.0.2",
    "dotenv": "^8.2.0",
    "eslint": "^7.8.0",
    "eslint-config-bliss": "^5.0.0",
    "gh-pages": "^3.1.0",
    "jest": "^26.4.2",
    "ts-jest": "^26.2.0",
    "typedoc": "^0.17.0-3",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@types/node-fetch": "^2.5.7",
    "form-data": "^3.0.0",
    "node-fetch": "^2.6.0"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "deploy": "gh-pages -d docs && rm -rf docs",
    "docs": "typedoc --out docs index.ts --readme none --mode library",
    "lint": "eslint . --ignore-pattern dist",
    "lint-fix": "yarn lint --fix",
    "test": "yarn build",
    "_version": "yarn build && yarn docs && yarn deploy"
  },
  "eslintConfig": {
    "extends": [
      "bliss"
    ]
  },
  "renovate": {
    "extends": [
      "bliss"
    ]
  },
  "keywords": [
    "freesound",
    "node",
    "brower",
    "v2"
  ],
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
